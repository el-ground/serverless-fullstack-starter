import { Push_vapidKey } from './vapid-key'
import { Push_registerPushToken } from './register-push-token'

export const PushQueries = {
  Push_vapidKey,
}

export const PushMutations = {
  Push_registerPushToken,
}
